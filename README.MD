# HTTP Server from Scratch in C
<p>A very minimal Http server</p>
## How to run ?
- Clone the repo 
- Use `gcc` Or `make` to compile
- If your using ***gcc*** run `gcc -o server server.c`
- If your using ***make*** run `make server`
- Run it after compiling `./server`

