# HTTP Server from Scratch in C
A very minimal Http server
## How to run ?
- Clone the repo:
```bash
git clone https://github.com/xenxaarn/http-server.git
```
- Use ***gcc*** to compile:
```bash
gcc -o server server.c
```
- To run:
```bash
./server
```

